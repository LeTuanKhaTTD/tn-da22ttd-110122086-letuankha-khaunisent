# Models

Model weights are not committed to GitHub.

Place the fine-tuned PhoBERT model here:

```text
models/phobert-sentiment/
â”œâ”€â”€ config.json
â”œâ”€â”€ tokenizer files
â”œâ”€â”€ model.safetensors or pytorch_model.bin
â””â”€â”€ training_metadata.json
```
