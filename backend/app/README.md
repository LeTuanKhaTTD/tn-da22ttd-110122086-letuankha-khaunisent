# backend/app

Canonical backend package for the reorganized TikUniSent structure.

It is split into:
- `core/` for settings and constants
- `models/` for Pydantic schemas
- `api/` for FastAPI routes
- `services/` for business logic
- `utils/` for helpers
