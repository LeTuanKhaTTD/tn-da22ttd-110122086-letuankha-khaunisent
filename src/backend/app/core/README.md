# backend/app/core

Centralized configuration and shared constants for the backend.

- `config.py` reads environment values from `.env`
- `constants.py` keeps shared limits and sentiment metadata in one place
